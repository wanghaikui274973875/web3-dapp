# web3-dapp

Vue 3 + TypeScript + Vite + ethers v6（MetaMask / Sepolia）。

## 运行

```bash
npm install
npm run dev
```

复制 `.env.example` 为 `.env`，配置 `VITE_PERMISSION_STORAGE_ADDRESS` 后需重启 dev。

更多模板说明见 [Vue 文档](https://vuejs.org/guide/typescript/overview.html#project-setup)。
